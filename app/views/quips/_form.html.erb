<%= form_for(@quip) do |f| %>
<% if @quip.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@quip.errors.count, "error") %>
    prohibited this quip from being saved:</h2>

  <ul>
    <% @quip.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="situation-wrapper">
  <div class="situation-flex">
    <div class="context three">
      <h2 class="temp">Context</h2>


      <% @settings.each do |setting| %>
        <%= f.label "setting_id_#{setting.id}", setting.set do  %>
          <%= f.radio_button :setting_id, setting.id %>
          <%= "#{setting.set}"%>
        <% end %>
      <% end %>


    </div>

    <div class="context one">

      <h2 class="temp">Situation</h2>

        <% @contexts.each do |context| %>
          <%= f.label "context_id_#{context.id}" do %>
            <%= f.radio_button :context_id, context.id %>
            <%= "#{context.situation}"%>
          <% end %>
        <% end %>

    </div>

    <div class="context two">

      <h2 class="temp">Person</h2>

        <% @targets.each do |target| %>
          <%= f.label "target_id_#{target.id}" do %>
            <%= f.radio_button :target_id, target.id %>
            <%= "#{target.person}"%>
          <% end %>
        <% end %>
      </div>

  </div>
</div>

  <div class="field">
    <%= f.label :tips %><br>
    <%= f.text_area :tips %>
  </div>
  <div class="actions move">
    <%= f.submit %>
  </div>
  <% end %>
